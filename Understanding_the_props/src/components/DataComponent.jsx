import elephant from '../images/elephant.jpeg';

const ImageData = () => {
  const imageArray = [
    { id: 1, img: elephant },
    { id: 2, img: elephant },
    { id: 3, img: elephant },
    { id: 4, img: elephant }
  ];
  return imageArray;
};

export default ImageData;
